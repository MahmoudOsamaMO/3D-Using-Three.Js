@model IEnumerable<PIS3D.Models.EaveConditionEnum_Table>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.EaveConditionEnum)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.EaveConditionEnum)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.ID_EaveConditionEnum }) |
                @Html.ActionLink("Details", "Details", new { id=item.ID_EaveConditionEnum }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.ID_EaveConditionEnum })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
